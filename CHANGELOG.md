# 更新记录

[返回使用说明](README.md)

以下按版本记录当时的行为，当前使用方法以 README 为准。

## 0.8.11 回复选项双模式

新增“创意写作”模式，以作者 / 编剧视角设计可择一采用的剧情推进方案；保留原有角色扮演模式。两种模式分别保存方向与生成要求，作者模式隐藏角色人称及对白形式设置，支持展开输入框里的作者构想。OS 与楼层窗口均提供模式切换，点击仍仅填入、支持撤销、不自动发送。219 项自动测试通过。

## 0.8.10 类型转换需用户授权

世界状态 → 生成设置新增“允许更改已有字段类型”，默认关闭。未勾选时，更新要求或用户状态规则可通过“允许更改已有字段类型”明确授权；也可写“把体力改为数字”，只授权对应字段与目标类型。使用明确措辞，未识别的表达不会自动放行。角色卡、世界书和剧情内容不构成授权。

提示词与写入合并均执行限制，模型擅自改变未授权字段的类型会整批拒绝。“待确定”占位值同样受限制。配置与任务一起捕获，保留并发冲突检查。217 项自动测试通过。

## 0.8.9 状态栏 AI 更新允许转换类型

解除状态栏 AI 更新提示词及合并校验中的固定类型限制。用户明确要求转换，或占位文本已有可靠具体值时，可在文本、有限数字、布尔、字符串列表、进度对象之间转换。进度对象仍需满足当前值与最大值约束，不支持任意嵌套对象。

保留禁止新增变量、非法结果整批拒绝及生成期间并发修改保护。正文末尾的 state 协议由宿主解析，其类型约束不在本次修改范围内。216 项自动测试通过，实际模型尚待用户环境验证。

## 0.8.8 能力与信息面板楼层入口

消息楼层新增能力面板、信息面板按钮及完整应用窗口。按钮顺序固定为地图、状态、回复选项、能力、信息；窗口按打开顺序排列，关闭重开移到末尾。设置 → 楼层窗口包含五个显示开关及各自尺寸覆盖，外观继承对应应用。

新入口显示和编辑当前聊天的数据，不是历史楼层快照。各入口共享应用服务，避免重复注册正文提醒；关闭窗口清理界面监听与取消其未完成任务。已在浏览器模拟宿主检查展开及重开排序，真实酒馆仍需验证。

## 0.8.7 累计修改前后记录与重置

信息面板每次确认应用都保存修改前后快照。后续正文请求按顺序附带所有有效修改的前后资料及差异，同一字段以最后一次修改为当前状态；未改字段仅作为上下文。没有实际字段值变化的保存不增加提醒。未知和推测字段不作为事实发送。

“资料面板 → 一键重置当前面板修改”恢复本轮累计修改前的资料，同时清除当前面板正在发送的累计记录，其他面板不受影响。历史版本保留，重置后再次修改从新的记录开始。历史页可展开查看前后完整快照。超过提醒字符上限会明确报错而不会静默截断。

## 0.8.6 信息面板资料收纳与修改提醒

成功生成的资料自动保存到当前聊天的独立资料库，切换角色再返回或刷新后可从“资料收纳”打开；不同聊天互相隔离。同名同类型对象归入一组，字段分类默认折叠。编辑中的资料也可点击“保存资料（不影响正文）”保存。

资料保存和剧情应用分开。只有确认应用的改动字段和删除项会加入后续正文提醒；单纯查询、生成和保存资料不注入正文。恢复原始字段值后解除对应覆盖。旧版本已经丢弃的未保存草稿无法恢复。

## 0.8.5 多套 API 配置与模型列表

AI 设置顶部可选择已保存配置，切换后应用于后续生成；支持命名另存、更新（含重命名）和删除。地址、模型、密钥记忆选项、输出长度、超时、流式与队列策略分别保存。删除配置不清空当前正在使用的设置。密钥默认只保留在页面会话中，勾选“记住密钥”后才跨刷新保存。

“拉取模型列表”通过当前填写的接口独立 GET /models，支持从完整 /chat/completions 地址推导，模型名去重后可直接选择，也可继续手动输入。拉取失败不会覆盖已填写模型。修改接口或密钥会取消旧列表请求。继承酒馆模式选择列表中的模型时，会复制当前连接到手动配置，不修改酒馆设置；保存后生效。

## 0.8.4 读取酒馆配置并独立请求

共享 AI 不再调用酒馆生成流程。默认读取酒馆当前 Chat Completions 的地址、模型与可读取密钥，由 Amin os 独立发送请求；支持自定义兼容接口、OpenAI（含反向代理密码）和 OpenRouter。手动接口仍可使用，输出长度、流式与任务队列继续由共享 AI 设置控制。

密钥读取受酒馆自身权限限制，不修改服务端权限，也不会回退到酒馆生成。若密钥无法读取，请选择“手动配置直连接口”填写相同地址、模型和密钥。未支持的供应商协议及自定义额外请求头/请求体会明确提示。浏览器直连接口需要允许跨域请求；取消会中止插件请求。密钥不写入任务预览或聊天。

通用超时提示改为“模型等待超时，已停止等待；未应用生成结果”，信息面板不再显示“地图未修改”。保留切换应用后台继续、切换聊天取消与过期结果保护。

## 0.8.3 Windows 10 浅色与文字配色

设置 → 整体外观 → 窗口主题新增明确的 Windows 10 浅色、深色选项。浅色使用浅灰窗口、白色内容区和深色文字；深色的品牌图标、楼层入口、设置文字与链接采用浅色文字。蓝色继续用于主要按钮、焦点和选中线。两种主题仍支持单应用独立外观。

## 0.8.2 后台检索与 Windows 10 窗口

信息面板生成与模拟期间切换标签页、返回主页、打开其他应用或收起 OS 不再取消任务，返回可看到进度和生成草稿。完成后不会把正在查看的历史或设置页强制切走。真正切换聊天仍取消旧任务；聊天内容或面板版本变化时拒绝过时结果。主动取消和修改当前任务相关内容仍会中止任务。

Windows 10 磁贴风格默认自动搭配深色直角窗口、紧凑标题栏、蓝色选中标签和统一控件；单个应用的独立主题仍保留。设置 → 整体外观 → 窗口主题可选保留原主题，主题列表也新增 Windows 10 深色。

## 0.8.1 信息面板角色来源修正

「读取当前角色卡及其绑定世界书」同时包含角色卡正文、主绑定书、角色附加书，以及没有主绑定书时的内嵌世界书。群聊读取当前群成员的对应来源；与手动勾选世界书去重，不自动加入无关全局书。停用条目继续过滤。仅读取角色来源时不必先刷新世界书列表；额外全局、聊天等书仍可刷新后自行勾选。互联网单独检索模式不读取这些本地资料。

## 0.8.0 分层背景与动态磁贴

设置 → 背景与动效：可选纸页纹理、极光山影或自定义图片，调整遮罩、磁贴不透明度、边框与阴影。动效可选樱花或自定义视频层，限制在磁贴区域，图标和文字处于上层。樱花数量、速度、透明度可调；视频静音循环。编辑布局、离开主页、收起窗口和系统减少动态设置会暂停播放。默认保持原外观，所有效果均可关闭。

主页 → 编辑布局 → 背景图片地址可为每一块磁贴单独指定图片，复制磁贴会复制图片设置。支持 http(s) 地址与 / 开头的酒馆资源路径，不接受电脑磁盘路径。视频需提供可直接播放的地址，透明效果取决于视频本身是否带透明通道；不透明视频可调低动态层不透明度。网络、服务器限制、浏览器格式和自动播放策略可能影响自定义媒体，失败时保留可操作主页。内置纸页与山影为轻量程序纹理，不包含参考图中的骑士插画或山景照片。

## 0.7.3 拖动调整窗口高度

OS 窗口远离悬浮入口的一侧新增居中短横把手：通常在顶边，窗口向下展开时在底边。上下拖动即可拉长或缩短，手机也可拖动。手动高度对主页与应用通用，保存在本设备浏览器，刷新后保留；屏幕不足时自动限制高度。双击把手恢复自动高度，聚焦后可用方向键调节，Home/End 切换最小/最大可用高度。取消拖动恢复之前高度。在设置里更改侧栏最大高度会重新使用设置值。

## 0.7.2 竖向长磁贴与 Windows 10 风格

主页编辑布局新增「长」尺寸，占两列、四行，是「宽」磁贴的竖向版本，支持拖动、复制与保存。设置 → 整体外观 → 主页磁贴风格可选择「风格 2 · Windows 10」：深色开始屏、彩色直角磁贴、居中图标与底部标题。应用内部主题不受影响，也可随时切回原风格。现有布局和默认风格保持不变。

## 0.7.1 自定义主页磁贴

主页 → 编辑布局：每块磁贴可以选择打开的应用、自定义名称和尺寸。可以新增、复制、删除或撤销最近一次删除，同一应用可配置多个入口。拖动排序和前移/后移保留。重复磁贴打开同一应用页面，共享该应用的数据，不创建独立副本。

旧布局自动迁移并保留顺序与尺寸；新布局保存到本设备浏览器，删除的磁贴不会刷新后重新出现。主页下方「所有应用」始终可以进入各应用，即使已删光磁贴也可继续使用和新增。设置中可恢复默认磁贴布局。

## 0.7.0 独立信息面板

新增人物、事物、世界三类资料面板，沿用 OS 统一主题、按钮、表单与磁贴布局。与能力应用分别存储，不直接改动世界书、地图或状态栏。

- 在「生成资料」填写目标和要求，选择「酒馆资料」「互联网」或「两者」。酒馆来源可选择当前可用的世界书、角色卡和聊天；先刷新世界书列表。AI 规划关键词后检索相关片段，并显示检索范围、来源和未纳入数量。资料预算有限，不保证查全所有信息。
- 默认只整理来源；勾选允许创作补全后，可按要求生成资料里没有的设定。字段分别标注有来源、推测、新增设定、未知和手动改写。引用校验只验证原文存在，不能保证模型解释正确。
- 生成结果先进入草稿，可修改分类和任意字段、增删属性。重新检索保留手动改写、已采用新增设定及已删除字段。先「预览改写」再确认应用；模拟演变和采用建议也只改变草稿。
- 应用后的设定保存在当前聊天元数据中，可查看历史版本并加载为回滚草稿。每轮正常聊天生成使用独立提醒；未知和推测不会作为确定事实注入。可选择一直成立的设定重构或从当前时刻生效。它通过提示影响后续故事，不改写已有聊天文本。聊天分支变化会排除不属于当前分支的记录。
- 「检索设置」可以关闭剧情提醒、调整长度并预览内容。联网搜索使用 Tavily Search 格式接口，可填写兼容代理地址和密钥；密钥只在当前页面内存中，刷新需重填，不写入聊天或 AI 预设。外部检索只发送目标或用户填写的搜索词；选取的资料交由 OS 共享 AI 整理。

联网模式需配置搜索服务密钥，模型 API 密钥不能替代搜索密钥。浏览器跨域策略不允许直连时需兼容代理；失败会明确报错，不伪装成成功搜索。接口格式参考 [Tavily Search 官方文档](https://docs.tavily.com/documentation/api-reference/endpoint/search)。真实酒馆、真实模型与实际联网搜索尚需用户环境验证。

## 0.4.1 楼层回复选项与统一控件

修正磁贴编辑栏下拉框与按钮的主题配色，避免宿主暗色控件样式覆盖。消息楼层新增回复选项按钮，与地图、状态按钮共用原有样式和对齐设置。

设置 → 楼层窗口新增三个按钮的显示开关，由 OS 统一管理；关闭开关会收起对应已打开窗口。回复窗口沿用统一宽高、圆角与主题，也可单独设置尺寸。窗口按每条消息内的打开顺序从上到下排列；关闭再打开会排在末尾，按钮顺序始终为地图、状态、回复选项。

楼层回复选项复用共享 AI 设置和草稿保护，参考到指定楼层为止的聊天，生成候选后填入当前输入框，不自动发送。多个楼层使用独立窗口，关闭时取消等待并移除监听。旧楼层内容被替换或聊天切换时清理旧窗口。世界书与角色设定仍使用当前配置，不恢复历史版本。

已在模拟宿主中验证生成、填入、撤销、窗口打开顺序、关闭重开、显示开关与 390px 手机排版；真实酒馆与实际模型仍需更新后试用。

## 0.4.0 Metro 磁贴开始屏

首页采用 Windows Phone 风格的直角磁贴：小、中、宽三种尺寸，四列网格自动排布。地图默认宽磁贴，世界状态和回复选项默认中磁贴，AI 设置和设置默认小磁贴。配色跟随全局主题，Courgette 品牌名称保留。

点击“编辑布局”或长按约 450ms 进入编辑模式；拖到另一块磁贴上松手即可插入排序，目标磁贴显示虚线提示。编辑时点击磁贴选中它，在上方切换尺寸；也可使用前移/后移按钮或方向键排序。点击“完成”后恢复点击打开应用。设置 → 整体外观 → 恢复磁贴默认布局可恢复初始顺序及尺寸。

布局保存于当前浏览器/设备的本地存储，刷新后保留，不跨设备同步。进入应用显示返回键和当前应用名称。此版本改造开始屏与导航，应用内部表单和楼层窗口沿用原有设置；尚未加入动态信息磁贴。

验证：浏览器鼠标拖动、尺寸切换、刷新恢复、默认布局重置与应用返回已通过；390px 窄屏无横向溢出。已实现触屏长按/拖动处理，但尚未在真实手机上验证触屏手势。

## 0.3.10 Courgette 品牌字体

顶部和悬浮入口改用本地 Courgette 字体，不再使用图像字标遮罩。顶部字号 20–26px，悬浮入口 13–15px，随全局字号有限调整；颜色继续跟随主题。字体及 SIL OFL 许可证位于 assets/fonts/Courgette-*，其余应用文字保持原样。

## 0.3.9 字标加粗

为连笔字标遮罩增加轻量轮廓扩张，增强小尺寸可读性，保留笔画粗细变化。顶部与悬浮入口同步，尺寸与主题配色保持原样。

## 0.3.8 连笔字标

使用已确认的生成书法字样，通过 SVG 图像遮罩跟随主题文字色；它是图像字标，不依赖系统字体。顶部 120 × 40，悬浮入口 76 × 26，入口图标同步缩小以留出边距。保留按钮无障碍名称。

## 0.3.7 书法品牌字

顶部与悬浮入口的 Amin os 使用随插件附带的 Allura 英文书法字体，其余文字保持原样。颜色仍随主题变化。字体来自 Google Fonts / Allura Project，采用 SIL Open Font License 1.1，许可证位于 assets/fonts/Allura-OFL.txt。

## 0.3.6 精细品牌标志

轨道改为粗细渐变、两端收尖的曲线轮廓，分为两段环绕弧；星芒采用内凹弧线。顶部使用精细版，悬浮入口使用加粗简化版，均随主题重点色变化。

## 0.3.5 星芒与轨道位置修正

重绘品牌标志内部几何：星芒与倾斜椭圆轨道共用中心，缩小星芒并留出间隙，解决星芒贴在轨道下沿的问题。顶部与悬浮入口同步，颜色仍跟随主题。

## 0.3.4 图标居中

收起按钮改用居中的 SVG 箭头，避免字体基线导致偏下；校正轨道标志的垂直留白，约束宿主样式对图标定位的影响。

## 0.3.3 精简首页

移除英文副标题、欢迎文案和卡片常驻说明，说明改为悬停提示。保留应用名称与两列布局，卡片更紧凑，首页高度随内容收缩；打开应用恢复设置中的窗口尺寸。继续跟随所有全局主题与外观设置。

## 0.3.2 自适应品牌图标

顶部和悬浮入口采用轨道星芒矢量标志，随全局主题重点色自动变色；去掉斜体 a、图标方框和入口尾部圆点，调整垂直居中与文字间距。继续使用统一的圆角、透明度、模糊与字号设置，保持窗口定位和拖动行为。

## 0.3.1 楼层窗口与按钮

地图和状态入口合并到每条消息的一条工具栏，按钮统一样式并紧邻排列。设置 → 楼层窗口可指定靠左、居中或靠右，同时控制按钮与窗口对齐。

两个窗口默认共用最大宽度和桌面/手机高度。默认“统一高度”使两个展开窗口等高；也可选择随内容变化，或分别为地图、状态覆盖尺寸。旧版楼层宽高设置会保留并成为共同默认值。窗口仍不超过消息容器宽度，手机自动使用手机高度。地图内部画布、详情和控件圆角现已跟随外观设置。

本次不修改状态内容排版、地图数据或历史记录。验证了工具栏可见性/清理、尺寸继承与覆盖；演示浏览器桌面两个窗口为 720 × 468，靠右设置后同为 600px 宽，390px 窄屏两窗等宽等高且不横向溢出。

## 0.3.0 统一外观设置

新增“设置”应用，包含整体外观、单个应用和楼层窗口三页。

- 整体外观：六种统一主题，字号、圆角、舒适/紧凑间距、背景不透明度、模糊，以及侧栏宽度和最大高度。屏幕空间不足时自动缩小。
- 单个应用：地图、世界状态、回复选项、AI 设置默认跟随全局；可选择独立外观。删除覆盖后恢复跟随。设置应用自身跟随 OS。
- 楼层窗口：状态记录跟随世界状态外观，桌面按消息容器宽度自动并排卡片，窄消息/手机改为单列。最大宽度、桌面和手机高度上限集中设置。窗口不超过消息宽度，长文本换行，超出高度内部滚动。

地图、状态原有外观选择器改为“打开设置 · 统一外观”。旧主题/玻璃/装饰风格配置不再作为 Amin os 的外观来源，原存储保留以便回退；简化为统一主题、圆角、间距和透明背景。消息按钮开关等功能设置仍留在对应应用。新配置保存于酒馆扩展设置 `amin_os_appearance_v1`，不修改地图、聊天变量、生成配置和历史数据。

保存立即更新已打开的界面及状态 iframe，不重新创建编辑表单。样式仍限定在 Amin os、其地图窗口和状态楼层组件内，不更改酒馆主界面。

