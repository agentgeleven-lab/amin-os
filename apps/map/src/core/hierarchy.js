export const GENERATION_LEVELS=[
    {id:'world',name:'世界／大陆',prompt:`尺度与边界：以当前世界或大陆为范围，节点表达国家、大区域、主要城市和跨区域枢纽。先明确采用行政分区还是自然地理分区，名称与说明体现层级，不能将同一地区的概括节点和大量内部细节无区别地混排。
覆盖清单：依据素材逐区梳理大陆、国家或文化地理区；再检查首府、主要聚落、边境关口、区域港口、山脉通道、重要岛屿及跨区交通枢纽。只纳入确有依据且影响宏观通行的类别，不要求每个世界都有所有类别。不能只围绕主角当前城市，也不能把全部地点机械串成一条线。
连接规则：体现区域邻接、陆路走廊、海运或设定中的跨域交通；有山海、封闭边界等障碍时说明通行条件，未知关系保留缺口。道路说明区分直接连接与省略普通沿途地点的概括区间，不把后者写成直接相邻。
排除内容：不要混入房间、单栋建筑、城市内部街道、店铺、家具。资料中的内部地点可归入所属城市说明，留给子地图。非当前大陆的地点仅作出境方向说明，不擅自扩大本图边界。`},
    {id:'region',name:'国家／省份',prompt:`尺度与边界：仅生成指定国家、省份或地理地区内部的城镇、山川和交通路线。先识别该地区的主要分区，在同一尺度上逐区检查，不能用一个核心城市替代整片地区。
覆盖清单：按资料检查主要城市、相关城镇与村落、行政或自然分区、山川湖泊与通行障碍、边界关口、区域车站、港口和其他交通枢纽，以及对区域剧情或出行重要的独立设施。剧情明确提及、用户点名的地点优先保留；不同类别不作为必须凑齐的清单。
连接规则：梳理城镇之间的干线、通往支线聚落的分支和必要换乘；共享枢纽只生成一次。兼顾各分区，不只生成两三座知名城市。山川等区域性对象仅在有地理或路线意义时成为节点，不能当成所有道路必经的虚构中转站。
排除内容：不生成其他国家或范围外城市，不展开室内房间、家具、城市内每条街道。边界之外的去向在说明中标注；源资料仅提供小范围地点时承认覆盖不足，不凭空发明完整行政体系。`},
    {id:'city',name:'城市／聚落',prompt:`尺度与边界：仅生成指定城市或聚落内部的街区、建筑、广场及街道，以街区与可访问设施为主体，不把其他城市作为内部地点。
覆盖清单：按有依据的街区检查居住、商业、公共服务、行政、教育、医疗、文化休闲、生产仓储、交通出入口与公共空间；结合世界背景取舍，不把现代设施强加给古代或奇幻聚落。尤其核对角色住所、工作学习地点、常去场所、剧情发生地和用户指定地点。不能只围绕一个车站生成全部地图。
连接规则：主要街区通过道路与枢纽形成骨架，具体设施合理接入所属区域；检查步行接驳、桥梁、城门、车站入口及分支，不因名称相似就推定相邻。没有精确地址不编造门牌或精确步行距离。
排除内容：不生成城外其他城市、全国交通网或建筑内部每个房间。小型聚落不强行增加机场、地铁、医院等类别。地标与所属街区可共存，但说明包含关系，不把包含关系当成一段已知距离的道路。`},
    {id:'site',name:'宗门／园区',prompt:`尺度与边界：仅生成指定宗门、校园、基地、庄园或园区内部的功能区域、建筑与内部道路，以围界和入口资料限定范围。
覆盖清单：按用途检查主次入口、对外接驳、主要办公或议事区、居住休息区、教学修炼或生产区、服务后勤、仓储、医疗或应急设施、公共活动区及资料明确的禁区；只采用与本园区类型和设定相符的类别，不预设宗门一定有藏经阁或园区一定有实验室。
连接规则：从入口梳理主通路、分区支路、建筑入口、独立院落和受限通道；明确公开区与限制区之间的通行条件，未知安保机制不编造。功能上相关不等于物理直接相连。
排除内容：不扩展到周边城镇、全国道路或每栋建筑的室内细节；建筑内部留给子地图。未确认的秘密场所不自行创建或标记已发现。`},
    {id:'interior',name:'建筑／地下城',prompt:`尺度与边界：仅生成指定建筑或地下城内部的房间、楼层、出入口和通道。先按已知楼层、区域或地下深度组织，说明节点所属层次；范围只有一间房时按用户指定尺度表示，不擅自补出整栋楼。
覆盖清单：检查主次入口、门厅、走廊、楼梯或其他垂直通路、核心功能房间、生活与服务空间、储藏及维护区、安全出口；地下城依据设定检查分岔、洞室、门禁与已知机关。物品和家具通常写入房间说明，不为了增加数量变成独立地点。
连接规则：以门、走廊、楼梯等实际通路连接，检查每层的可达性及跨层衔接。上下层关系写在 description 或道路 metadata.note；协议中的平面 direction 仅作布局示意，不用它伪装真实上下关系，不添加协议不支持的方位值。明确单向或封闭通道时遵循设定。
排除内容：不要生成城镇、国家或外部区域；普通房屋不强行添加地下密室、宝库、怪物或隐藏通道。资料明确未发现的空间遵守 discovered 设置，不自动揭示剧情秘密。`},
    {id:'free',name:'主要地点／自由',prompt:`不受固定生成层级约束：以用户要求、角色卡、世界书和聊天中与故事有关的主要地点为中心，可以在同一张地图中放置城市、街区、建筑、房间、野外区域或跨区域地点，不强制统一空间尺度。
按剧情重要性挑选：优先用户明确指定、角色经常活动、剧情目标与关键事件涉及的地点，并保留必要交通枢纽。主要地点不等于固定少数几个；有多个重要地点时完整保留，不设默认数量上限，也不为覆盖所有行政分区而塞入大量无关地点。
不把当前地图标题、上级地图或入口自动当作生成边界；它们仅提供背景。用户明确指定的范围和排除项仍需遵守。全面／适中／简略在这里表示主要地点的收录深度，不要求填满城市或建筑分类清单。
不同尺度地点可共存，说明所属地区和包含关系；包含关系本身不等于道路。连接只表达可通行关系，未知距离留空，必要的推定注明依据；不把跨城或跨层移动伪装成直接相邻。不要因名称相似合并不同地点。
仍只生成当前这一张地图，保留其他地图、既有子地图入口 ID 与身份，遵守现有地图类型和 JSON 协议。新增模式只补地点和道路，不改写原内容。资料不足不强行编造地点，用户要求的创作内容应标注。提交前核对重要地点是否遗漏，不输出自检过程。`},
    {id:'custom',name:'自定义',prompt:`尺度与边界：严格按用户指定的空间范围与细节尺度生成。先从要求识别地域边界、节点代表什么、应包含及排除的内容、允许的依据与补充方式；要求不明确时结合当前地图路径缩小范围，不擅自扩展至上级区域。
覆盖清单：把用户列出的地点、分类、数量要求和连接要求逐项核对；按用户给定分组组织，不能套用固定的城市或国家模板。有明确数量目标但资料不足时优先保留可靠信息，不凑数编造地点。
连接规则：根据用户定义的空间语义组织路线，允许分支、闭环和独立区域，未知连接不强加。仍使用当前地图类型、类型目录和输出协议；自定义不代表可以输出额外地图、脚本或不受支持的字段。
排除内容：用户明确排除的地点不能重新加入。用户要求与当前入口范围明显冲突时，在地图说明中标注范围待确认；不得悄悄替换上级或其他地图。`},
];
export function mapPath(doc,id){const path=[],seen=new Set();while(doc.maps[id]&&!seen.has(id)){seen.add(id);path.unshift(doc.maps[id]);id=doc.maps[id].parentMap;}return path;}
export function mapChoices(doc){return Object.values(doc.maps).map(m=>({id:m.id,name:mapPath(doc,m.id).map(x=>x.name).join(' → ')})).sort((a,b)=>a.name.localeCompare(b.name));}
export function locationPath(doc){const map=doc.maps[doc.activeMap],path=mapPath(doc,map.id).map(m=>m.name);if(map.nodes[map.currentLocation])path.push(map.nodes[map.currentLocation].name);return path.join(' → ');}
export function projectedLocation(doc,mapId){let map=doc.maps[doc.activeMap];if(map.id===mapId)return map.currentLocation;while(map.parentMap){if(map.parentMap===mapId)return map.metadata.parentNode??null;map=doc.maps[map.parentMap];}return null;}
export function generationScope(doc,mapId,level){
    const map=doc.maps[mapId],parent=doc.maps[map.parentMap],entrance=parent?.nodes[map.metadata.parentNode];
    return {当前地图:{id:map.id,name:map.name,type:map.type,currentLocation:map.currentLocation,当前位置资料:map.nodes[map.currentLocation]??null},生成层级:GENERATION_LEVELS.find(l=>l.id===level)?.name,层级路径:mapPath(doc,mapId).map(m=>({id:m.id,name:m.name})),上级地图:parent?{id:parent.id,name:parent.name}:null,入口地点:entrance?{id:entrance.id,name:entrance.name,description:entrance.description}:null,必须保留的子地图入口:Object.values(doc.maps).filter(m=>m.parentMap===mapId&&m.metadata.parentNode).map(m=>map.nodes[m.metadata.parentNode])};
}
export function levelPrompt(level){const entry=GENERATION_LEVELS.find(l=>l.id===level);if(!entry)throw new Error('请选择有效生成层级');if(level==='free')return `\n本次生成层级：${entry.name}。${entry.prompt}`;return `\n本次生成层级：${entry.name}。${entry.prompt}\n以提供的层级路径和入口地点说明限定范围。上级地图只提供背景，不属于本次重建目标。必须保留已有子地图入口的 ID 和地点身份。只生成当前一张地图。
覆盖自检：生成前按“范围 → 分区 → 地点类别 → 交通骨架”在内部整理候选清单；提交前核对用户点名地点、素材中范围内明确地点、各主要分区和必要入口是否遗漏。类别是检查维度，不是凭空补齐的配额；有依据优先，数量服从实际尺度。
同一地点与别名合并，不同分区的同名地点区分；不得把地点概括成一个节点而丢掉明确要求的其他地点。连通性不足时标明未知原因，不强行补路。完整生成保留子地图入口；新增模式只补遗漏，不删除或重写既有节点。
资料不足或范围冲突时，只在相关地点 description 或道路 metadata.note 简短标注待确认；输出仍严格遵守 JSON 协议，不额外输出自检过程，不宣称已穷尽全部地点。`;}
